doctype html
html(lang="en")
  head
    title oss-pulse
  body
    #app(style="text-align: center; margin-top: 16em")
      if user
        p= user.username
        a(href="/auth/logout") Logout
      else
        a(href="/auth/github") Login

    script(src="bundle.js")
